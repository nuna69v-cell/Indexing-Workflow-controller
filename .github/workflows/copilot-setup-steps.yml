name: Copilot Setup Steps

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  setup-copilot:
    runs-on: ubuntu-latest
    environment: copilot

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ secrets.COPILOT_PYTHON_VERSION || '3.13' }}

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y sqlite3 libopenblas-dev
          if [ -f ta-lib/ta-lib_0.6.4_amd64.deb ]; then
            sudo dpkg -i ta-lib/ta-lib_0.6.4_amd64.deb || sudo apt-get install -f -y
            sudo ln -s /usr/lib/libta-lib.so /usr/lib/libta_lib.so || true
          elif [ -f ta-lib_0.6.4_amd64.deb ]; then
            sudo dpkg -i ta-lib_0.6.4_amd64.deb || sudo apt-get install -f -y
            sudo ln -s /usr/lib/libta-lib.so /usr/lib/libta_lib.so || true
          fi

      - name: Install Python Dependencies
        env:
          TA_INCLUDE_PATH: /usr/include/ta-lib
          TA_LIBRARY_PATH: /usr/lib
          LDFLAGS: "-L/usr/lib"
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
